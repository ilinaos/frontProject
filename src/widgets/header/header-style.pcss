.header {
    background: var(--white);
    max-width: 1280px;
    display: flex;
    justify-content: space-between;
    box-shadow: 0px, 2px, 10px, 0px, var(--neutral-grey-light);

    &__logo {
        width: 30%;
        padding: 0.5em;
    }

    &__nav {
        display: flex;

    }
}
